// Make sure this is a client component if it needs to be
import BillingsCalculatorLanding from "../../billings-calculator-landing"

export default function LandingPage() {
  return <BillingsCalculatorLanding />
}

